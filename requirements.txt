# ──────────────────────────────────────────────────────────────────────────────
# ASM Asset Management System — Python Dependencies
# All versions are pinned for reproducible builds.
# ──────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.37
psycopg2-binary==2.9.10
alembic==1.14.1

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.0

# AI / LangChain
langchain==0.3.14
langchain-core==0.3.33
langchain-openai==0.3.3
langchain-community==0.3.14
openai==1.59.4

# HTTP client (used by AI tools to call the local API)
requests==2.32.3
httpx==0.28.1

# Environment / config
python-dotenv==1.0.1

# Utilities
python-dateutil==2.9.0

# ──────────────────────────────────────────────────────────────────────────────
# Development / Testing (not installed in the production Docker image)
# Install with: pip install -r requirements-dev.txt
# ──────────────────────────────────────────────────────────────────────────────
# pytest==8.3.4
# pytest-asyncio==0.25.0
# httpx==0.28.1   (already above, shared)
# coverage==7.6.10
